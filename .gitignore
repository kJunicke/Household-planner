# Dependencies
node_modules/

# Environment variables
# CRITICAL: NEVER remove .env from gitignore!
# Contains SUPABASE_ACCESS_TOKEN with full admin rights
.env
.env.local
.env.*.local

# Claude Code local settings (contains personal file paths)
# Note: Git is configured to use GitHub noreply email (kJunicke@users.noreply.github.com)
.claude/settings.local.json

# Playwright MCP screenshots and test artifacts
.playwright-mcp/
